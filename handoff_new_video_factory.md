# 新一代微信视频号技术内容工厂 Handoff

更新时间：2026-08-20

## 1. 项目结论

`wechat-dispatcher` 是一次有价值的探索，但现有成片质量不达标。问题不是缺少工具，而是旧流水线把来源截图、译文、观点和字幕堆在静态卡片上，缺少真正的讲解镜头、声画节奏、页面操作、重点指向和素材层次。

新项目可以完全重建，不需要兼容旧的抓取、Pillow 渲染和调度逻辑。

目标不是“所有内容一键自由生成”，而是建立：

```text
内容发现 → 来源核验 → 类型化脚本 → 素材采集 → 讲解镜头 → 音频/字幕 → 成片 → 质量评估
```

MoneyPrinterTurbo 作为媒体后处理和公共素材能力；Playwright/web-scroll-video 负责真实网页和产品操作；`video_github_essay` 提供分镜导演模型；新项目自己维护事实、脚本、证据和镜头协议。

## 2. 微信视频号约束

### 2.1 已确认的产品方向

- 视频号是微信生态内的社交视频产品，好友互动、评论、转发和内容质量会影响传播；公开资料也强调了完播和社交传播的重要性。[腾讯视频号产品介绍](https://www.tencent.net.cn/zh-cn/products/channels/)、[微信视频号推荐原理资料](https://shipinhao.ifanr.cn/fans/mechanism.html)
- 不能把网上流传的“完播率 45%”“点赞率 3%”等数字当作官方固定门槛；它们只能作为历史经验或实验参考。
- “快报 10 秒以内”是本项目的内容策略假设，不是平台硬规则。必须通过实际账号数据验证。
- 视频号观看场景通常发生在信息流和社交转发中，因此前 1–2 秒必须让观众知道主题，画面不能长时间等待网页加载或展示空白。
- 画面需在手机小尺寸下可读：单幕只保留一个主要信息，中文大字幕、强对比、明确主体，避免把完整网页缩成不可读的小截图。

### 2.2 首版时长分层

| 格式 | 目标时长 | 用途 |
|---|---:|---|
| Flash 快报 | 6–10 秒 | 一个事实、一个数字、一个变化；适合快速测试完播 |
| 标准讲解 | 20–45 秒 | 工具、模型、官方公告、X 实践观点 |
| 深度带读 | 45–90 秒 | GitHub 项目、论文、Benchmark、公司技术路线 |

首版不把所有内容压缩成 10 秒。10 秒只适合信息单一的快报；复杂内容若强行压缩，会牺牲证据和可信度。

### 2.3 每条视频的实验指标

记录以下指标，不预设永远有效的阈值：

- 1 秒留存、3 秒留存、完播率
- 平均观看时长、重复观看率
- 点赞率、评论率、转发率、收藏率
- 关注转化、评论中出现的具体问题
- 不同视频时长、开场画面、字幕密度和素材类型的 A/B 结果

每周根据真实数据调整快报长度、开场结构和字幕节奏，而不是根据传闻算法反复改模板。

## 3. 素材分层原则

每个镜头必须声明素材角色：

```text
proof        事实证据
illustration 示意/公共素材
explanation  图表、代码、流程、标注
transition   氛围和节奏过渡
```

- `proof` 必须来自原帖、官网、文档、论文、GitHub、Release 或真实运行结果。
- `illustration` 可以由 MoneyPrinterTurbo 素材库提供，但必须记录来源、授权、下载地址和“示意画面”状态。
- `explanation` 根据已核验事实生成，不能添加未经来源支持的数字。
- `transition` 不承载事实。

例如数据中心主题：官方公告证明“发生了什么”；公共机房视频解释“数据中心是什么”；官方数字生成图表；旁白明确“画面为示意素材”。公共素材不能伪装成该项目的现场画面。

## 4. 工具分工

### MoneyPrinterTurbo

复用其以下能力：

- 公共素材搜索、下载、缓存
- BGM 混音
- 本地视频片段拼接
- 转场和基础视频效果
- MoviePy/FFmpeg 输出

先建立 `MPT Assembly Adapter`，不要直接复制整个 WebUI、任务系统和主题生成系统。当前适配器接收自己的镜头清单和视觉母版，采用无旁白、仅 BGM；MPT 的旧时间轴若会截断或循环浏览器录制，工厂保留自己的视觉母版，只复用其音乐轨。

如果内部接口不适合自定义证据画面，再从 MIT 代码中复制稳定的素材、字幕、音频和转场模块，并保留许可证和原作者声明。

### web-scroll-video

用于官网、README、文档和产品页的真实录屏。使用 cue sheet 描述：打开页面、等待、点击、输入、滚动、缩放、高亮。它输出 H.264 MP4，适合成为 `WebCapture`。

### Playwright

用于：

- X 原帖截图和动态页面读取
- GitHub 页面和文档交互
- 真实产品操作录制
- 获取 DOM 元素边界，生成激光笔/红框/放大区域

Playwright 不负责最终剪辑，只负责取得真实画面和可定位的视觉信息。

### video_github_essay

复用它的“分镜导演模型”：每幕同时定义口播、画面动作、花字、时间、录屏提示、重点和激光笔目标。

不要直接复用它的示例论文、项目、Benchmark 或互动数据；其中有演示型固定内容。它的激光笔目前主要是按场景序号映射到固定坐标，新项目应升级为 DOM 边界或图表区域驱动。

## 5. 统一内容协议

```text
Candidate
  source_type
  source_url
  author
  linked_sources[]
  published_at
  dedupe_key

Evidence
  id
  url
  quote
  captured_asset
  source_kind
  captured_at

Scene
  start
  end
  narration（可选编辑注释，不作为成片音频）
  caption
  screen_fact
  screen_interpretation
  highlight_translation
  evidence_ids[]
  material_role
  asset_query
  visual_action
  pointer_track
  zoom_track
  overlay_labels[]

RenderManifest
  candidate_id
  content_type
  scenes[]
  audio_path
  subtitle_path
  video_path
  source_urls[]
  license_records[]
  quality_checks[]
```

任何口播事实都必须能通过 `evidence_ids` 回到来源。没有证据的候选可以进入研究队列，但不能进入待发布队列。

## 6. 八类内容的实现方式

### 一、X 帖子 / 实践观点

来源：原帖、引用上下文、作者身份、被提到的原始链接。

画面：原帖真实截图/录制 + 相关工具页面或实际操作 + 观点标签。

叙事：严格区分“作者声称”“我们观察到”“尚不能推出”。个人经验不得写成普遍规律。人物、公司和厂商必须点名，并写清“谁做了什么、造成什么变化”。X 属于事件型入口，可以用新的原始证据关联同一主体近期发生的相关事件，例如把一次高管离职放回一段已被多来源证实的人才流动中；但单个事件本身不能被写成趋势。

### 二、GitHub 项目带读

来源：README、Release、源码、安装说明、真实运行结果。

画面：先真实录制仓库首页（名称、简介、文件树）和 README 顶部，再直接跳到两个关键模块；可补终端安装、输入输出或 Demo。完整 README 连续遍历是审核资产，不是成片主体。用激光笔指向文件、命令和关键代码，避免无目的地从上往下滚动。

叙事：解决什么问题 → 输入输出 → 安装接入 → 节省哪段劳动 → 试用任务 → 是否值得纳入工作流。默认积极呈现项目已经实现的能力；README 没有明确写出的限制、人工检查或“只适合原型”等判断不得自动补充。

开场先给事件与情绪，再给项目定义：内容 Agent 结构化返回项目主体/动作/影响、可选厂商背景、三段独立钩子、证据 ID 和稳定项目标题，导演编排为约 3 秒的“事件 → 能力揭示 → 观点/影响”冷开场。随后才进入真实 GitHub 页面，上栏恢复包含 repo 名称的项目标题。GitHub 不做自由行业发散；只有 README 或 README 直接链接的仓库文档明确提到厂商，而且厂商动作直接解释项目为何存在时，才允许讲“厂商做了什么、开源项目如何回应”。MoneyPrinterTurbo 这类没有因果背景的项目只讲项目本身。`strip/remove/clean/detect` 不得升级成“绕过、破解、攻破”。

### 三、新工具 / SDK / API / Agent

来源：官方文档、仓库、示例代码和本地试运行。

画面：旧流程与新流程对照、命令行、API 请求/响应、真实 Demo。

不能只展示宣传页，至少要有一个可复现的小任务。

### 四、新模型 / 新产品

来源：官方公告、模型卡、官方 Playground、官方 Benchmark。

画面：发布页、模型卡、官方 Demo、能力对比；未公布的内容明确显示“未知”。

不能把参数规模、单项榜单或营销措辞直接等同于普遍性能。

### 五、新公司 / 融资 / 团队动态

来源：官网、官方公告、可信原始文件和产品页面。

画面：产品路线图、技术栈、官网、团队公开信息。

不做投资建议，不从融资金额推断技术前景。

### 六、新论文 / 研究 / Benchmark

来源：论文 PDF、项目页、代码仓库、数据集说明、实验表格。

画面：方法图、关键公式、代码模块、Benchmark 表格、Failure Case；激光笔指向具体列和条件。

深度带读默认 45–90 秒，不为了 10 秒而删除实验条件和局限性。

### 七、官方新闻 / 产品公告

来源：官方原文、更新日志、迁移文档。

画面：新旧页面、文档变化、官方演示、迁移命令。

必须说明生效时间、覆盖范围、是否需要迁移。

### 八、帖子中提到的外部来源

X 只作为入口；如果帖子提到 GitHub、论文、产品页或公司，继续追到原始来源，形成关联证据节点。无法验证的外部来源只做候选，不自动成片。

## 7. 镜头和成片规则

- 前 1–2 秒出现主题、来源或结果，不显示空白加载。
- 上栏钩子必须同时包含主观态度和具体冲突/收益；态度允许鲜明，但事实部分必须有 `evidence_ids`，不得只写“爆了、颠覆、神器”。
- 每幕只保留一个主要信息，避免“截图 + 大段译文 + 多个数字”同时出现。
- Flash 每 1.3–2.8 秒发生一次语义和视觉变化；完整 X 原帖最多 3.2 秒。长内容仍以 2–5 秒为上限，但不为切换而重复同一事实。
- 证据画面优先可读性，公共素材只用于解释和节奏。
- 无旁白版必须让每幕在静音时可独立理解：显示事实/翻译与解释/影响；高亮外文时同时显示中文翻译。
- GitHub 先播放独立冷开场；进入真实带读后，固定深蓝黑上栏承载项目标题、固定下栏承载结论，中间是可变证据画面。分镜解释不得做成遮住证据的大卡片。
- 9:16 输出优先；必要时将网页录屏置于可读卡片内，配合背景扩展，不简单缩小完整桌面页面。
- 输出固定为 H.264/AAC、`yuv420p`、微信可播放 MP4，并用 `ffprobe` 自动验收。

## 8. 当前实现状态与后续优化

### Phase 0：媒体后处理验证（已完成）

选一个数据中心或模型公告主题，验证：官方证据 + 公共素材 + 邻近原文的中文释义 + 黄色重点标注 + BGM + MPT 最终编码。首版无旁白、无逐字字幕。

### Phase 1：核心采集器（已完成首版）

- `TweetCapture`
- `WebCapture`
- `GitHubCapture`

同时支持 X 快报、官方公告、GitHub 项目带读、普通网页与论文 PDF。X 先展示完整原帖；论文在没有系统 `pdftotext` 时使用项目依赖 `pypdf` 提取逐页文本，采集错误不得成为内容证据。

### Phase 2：真实工具和产品演示（统一内容协议已完成，真实案例继续回归）

加入本地 CLI/API 运行、浏览器产品操作、输入输出录制和失败回退。

### Phase 3：论文、Benchmark、公司和融资（统一内容协议已完成，图表/多模态案例继续回归）

加入 PDF 图表、方法区域、实验条件和局限性检查；这些内容默认走更长的讲解模板。

### Phase 4：数据反馈闭环（待接真实发布数据）

记录视频时长、开场类型、素材角色、字幕密度和发布后的完播/互动数据，持续调整模板。

## 9. 首个验收样片

同一套系统至少完成三条：

1. 数据中心主题：公共素材解释 + 官方数字证据。
2. 一个 GitHub 工具：真实安装和运行 + 激光笔带读。
3. 一个官方模型/产品公告：官方页面 + 实际开发者影响。

每条必须包含：来源清单、公共素材授权记录、内容 Agent 调用轨迹、分镜 JSON、BGM 授权记录、最终 MP4 和自动质检报告。

## 10. 内容 Agent 的成本边界

内容生成采用有限循环，不建设通用自主 Agent：

- 正常调用两次便宜模型：一次调研规划、一次结构化内容与分镜。
- 确定性校验失败时，允许同一便宜模型修复一次。
- 只有显式配置备用模型后，才允许更强模型兜底一次；每条总调用硬上限为四次。
- 模型决定讲什么、查哪条候选外链、选哪两个 GitHub 重点；工具负责归档证据、真实浏览器录制、合成和质检。
- 调研规划先输出 `EditorialOpportunity`：事件是什么、为什么是现在、为什么观众在意、选中它的证据理由和待验证扩展维度。选题理由必须进入最终故事，不能调研完就丢掉。
- 有限调研形成 `ContextGraph`；“像新闻”只体现在查清前情、时间、因果、竞争、人物流动、价格/能力变化等上下文，不把成片写成播报腔。上下文必须调查，但只有真正改变理解的节点才进入视频。
- X 快报额外检查同作者最近时间线，以主题重合度找出原帖默认读者已经知道的 setup；人物流动额外检查原组织此前变动，并按事件日期排除同日重复报道。`ContextGraph` 分开记录 required setup、incumbent pattern 和 discarded duplicate；人物流动存在可靠 pattern 时必须在画面中真正使用，不能只写进导演内部字段。
- 写作模型返回 `DirectorBrief` 和 `EvidenceShot`：导演简报负责观点、观众悬念、情绪强度与事件→背景→证据→影响→回报；证据镜头只描述事实、解释、来源和语义视觉家族。
- 外链页面同时提取并归档高价值来源图片（团队、产品、架构、Benchmark、结果）；公司/团队故事存在官方合影时必须真正使用，快报内同一张图片只出现一次。`editorial_evidence_coverage` 明确记录使用与舍弃的研究证据；只有进入 `evidence_shots` 的上下文才算观众可见，内部 story arc 不能冒充已经传达。派生卡不再使用无语义黄色横线，黄色只留给真实证据高亮和相邻翻译。
- 非 GitHub 模型不再返回底层 `kind`、Scene、浏览器 cue 或时长表。程序根据证据类型与 `visual_family/retention_job` 编译 `tweet_card/browser/pdf/code`，并统一安排 Flash 时长，彻底隔离“impact_card 被写成素材类型”这类错误。
- Flash 的可渲染视觉家族包括完整原帖、引用帖、真实官网/产品页/图表/代码，以及由已引用事实生成的 quote/timeline/impact/stat 节奏卡。同一来源同一处理方式连续出现不算视觉变化。
- GitHub 编辑角色（如 `trial`、`boundary`）只存在于 `github_brief.focus_candidates[].editorial_role`；底层 Scene 的素材类型不暴露给模型。
- 外链工具只能访问候选中已经发现的 URL，最多三个；抓取内容必须先归档和哈希，才能成为可引用证据。
- 上下文搜索每个导演查询先取一个可信来源，避免第一条宽泛查询吃完整个预算。Google News 跳转壳只归档媒体标题和日期，不再误当正文；写作提示总证据文本上限为 72,000 字符，防止单页脚本把请求推到数十万 token。
- 普通修复不再复制整份旧 JSON。若发现“不知道机制”、无证据的成本/API/配额推断、品牌名硬译、相邻事实重复等问题，便宜模型只返回字段级语义补丁；素材来源、锚点、顺序和渲染指令保持不变。
- 采集缓存与生成缓存相互独立：编辑 Schema 或门禁升级只让旧清单失效，不会让已经归档的 X 原帖重新访问登录浏览器；只有显式 `--refresh` 才重抓源站。
- 硬门禁包括：X 原帖相邻中文解释、引用帖时间关系、人物/公司归属、品牌名保持、数字与限制断言有来源、Flash 不以未知项收尾、相邻事实不近似重复、至少三种可渲染视觉家族，以及固定底栏不超过 62 个中文视觉字符。若结构修复后只剩翻译/底栏长度问题，可在四次总预算内追加一次低 Token 可见字段小修复。
- Agent 不能授予发布审批，也不直接编排 Playwright、FFmpeg 或 MPT 参数；只有质量门通过且人工批准了绑定内容摘要的批次后，确定性发布执行器才能提交该批次列出的平台。

## 11. 明确不做的事

- 不继续修补旧的静态卡片成片系统。
- 不让 LLM 自由生成无法回溯的事实。
- 不把公共素材冒充新闻现场或官方产品画面。
- 不把 MoneyPrinterTurbo 的整套产品系统作为新项目核心。
- 不把 10 秒当成所有内容的统一时长。
- 默认只自动生成待发布包；多平台提交是显式启用的后置能力，必须经过质量门、账号预检和一次可审计的人工批次审批。
